export default class User {
    private id: string;
    private name: string;
    private authType: string;

    constructor({name}) {
        this.setName(name);
    }

    private setName(name: string) {

    }
}